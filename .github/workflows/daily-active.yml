name: Daily Crypto Research

on:
  schedule:
    # 06:00 KST = 21:00 UTC (previous day)
    # Data refresh runs daily; Telegram is sent only on Saturday 06:00 KST.
    - cron: '0 21 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-research
  cancel-in-progress: false

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Determine Telegram delivery
        id: telegram_gate
        run: |
          if [ "${{ github.event_name }}" = "schedule" ] && [ "$(TZ=Asia/Seoul date +%u)" = "6" ]; then
            echo "send=true" >> "$GITHUB_OUTPUT"
            echo "Telegram report enabled for Saturday 06:00 KST"
          else
            echo "send=false" >> "$GITHUB_OUTPUT"
            echo "Telegram report disabled for this run"
          fi

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run daily pipeline
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SEND_TELEGRAM: ${{ steps.telegram_gate.outputs.send }}
          CRYPTOPANIC_KEY: ${{ secrets.CRYPTOPANIC_KEY }}
          COINGECKO_API_KEY: ${{ secrets.COINGECKO_API_KEY }}
          DASHBOARD_URL: ${{ vars.DASHBOARD_URL }}
        run: |
          if [ "$SEND_TELEGRAM" != "true" ]; then
            unset TELEGRAM_BOT_TOKEN TELEGRAM_CHAT_ID
          fi
          python -m src.main

      - name: Commit reports & snapshots
        run: |
          git config user.name  "crypto-research-bot"
          git config user.email "crypto-research-bot@users.noreply.github.com"
          git add docs/reports data/snapshots
          if git diff --staged --quiet; then
            echo "no changes to commit"
          else
            git commit -m "chore: daily report $(date -u +%F)"
            push_ok=false
            for i in 1 2 3 4; do
              if git push; then
                echo "push succeeded"
                push_ok=true
                break
              fi
              sleep $((2 ** i))
            done
            if [ "$push_ok" = false ]; then
              echo "::error::git push failed after 4 retries"
              exit 1
            fi
          fi

      - name: Notify failure
        if: failure() && steps.telegram_gate.outputs.send == 'true'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          MSG="⚠️ Crypto Research Agent pipeline failed on $(date -u +%F)%0ACheck: https://github.com/jinhae8971/crypto-research-agent/actions"
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${MSG}" \
            -d parse_mode="HTML" || true

  deploy-pages:
    needs: research
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs
      - id: deployment
        uses: actions/deploy-pages@v4
